"""Test package for fab-agent.

The actual tests live in ``test_validators.py``; this ``__init__`` is
intentionally empty so pytest can discover the module without
importing the full agent stack (which requires litellm / websockets).
"""
